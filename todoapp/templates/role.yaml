apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: todoapp-role
  namespace: todoapp
rules:
  - apiGroups:
      - ""
    resources:
      - pods
      - services
      - configmaps
      - secrets
    verbs:
      - get
      - list
      - watch
